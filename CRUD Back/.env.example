# Copie este ficheiro para .env e preencha com os teus valores locais.
# O ficheiro .env não deve ser versionado (está no .gitignore).

DATABASE_URL="postgresql://USUARIO:SENHA@localhost:5432/NOME_DO_BANCO?schema=public"
